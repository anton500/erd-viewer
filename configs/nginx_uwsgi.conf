upstream uwsgi {
  server erd-viewer:3031;
}

server {
    listen 80;
    listen [::]:80;

    uwsgi_read_timeout 6000;

    location / {
        root /usr/share/nginx/html;
        include uwsgi_params;
        uwsgi_pass uwsgi;
    }

    location /static {
        charset utf-8;
        root /usr/share/nginx/html;
    }
}